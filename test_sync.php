<?php require __DIR__."/vendor/autoload.php"; $app = require_once __DIR__."/bootstrap/app.php"; $kernel = $app->make(Illuminate\Contracts\Console\Kernel::class); $kernel->bootstrap(); $pages = App\Models\ProjectPage::all(); foreach($pages as $p) { echo "ID: " . $p->id . " | SLUG: " . $p->slug . " | TITLE: " . $p->title . PHP_EOL; }
